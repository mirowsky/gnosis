import type { StyledTabsProps } from "./StyledTabs";

export const DEFAULT_StyledTabs_TESTING_PROPS: StyledTabsProps = {
  activeIndex: 0,
  handleChange: () => {},
  tabLabels: ["Hello"],
};

export const StyledTabs_TEST_ID = {} as const;

import React from "react";

export type ContactDialButtonProps = {};

export const ContactDialButton = (props: ContactDialButtonProps) => {
  return <div>Hello, ContactDialButton!</div>;
};

export default ContactDialButton;

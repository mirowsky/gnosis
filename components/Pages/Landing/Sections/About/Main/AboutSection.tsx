import React from "react";

export type AboutSectionProps = {};

export const AboutSection = (props: AboutSectionProps) => {
  return <div>Hello, AboutSection!</div>;
};

export default AboutSection;
